
	<%= gravatar_for user, size: 64 %>
	<%= link_to full_name(user), user %> <br/>
	<%# user.rank FIX THIS user.house.name %>

	Trades: 0
	<% if current_user.admin? && !current_user?(user) %>
		| <%= link_to "delete", user, method: :delete,
									  data: { confirm: "You sure?" } %>
	<% end %>
